# Environment configuration for full local testing with Virtuoso
# Copy this to .env for complete local development setup

# === Core DSV Configuration ===
DSV_STORE_TYPE=virtuoso
DSV_STORE_PATH=./data/local-store
DSV_BASE_URI=http://localhost:8000/dsv

# === Virtuoso Configuration ===
# Local Docker instance (default settings)
DSV_VIRTUOSO_ENDPOINT=http://localhost:8890/sparql
DSV_VIRTUOSO_USERNAME=dba
DSV_VIRTUOSO_PASSWORD=dba
DSV_DEFAULT_GRAPH=http://localhost:8890/DAV

# === Testing & Development ===
DSV_DEBUG=true
DSV_LOG_LEVEL=DEBUG

# === Notes ===
# To use this configuration:
# 1. Copy to .env: cp env.testing .env
# 2. Start Virtuoso with dev mode: poetry run python docker-compose/manage.py start --wait --enable-dev
# 3. Run tests: poetry run pytest tests/test_integration.py -m integration -v
# 4. All SPARQL UPDATE and TTL loading functionality will work
